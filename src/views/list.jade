extend layout

block content
	h1 #{content_type}
	h3 This is a list view

	ul
		for item in list_items
			li
				if item.link
					a(href=item.link) #{item.title}
				else
					div #{item.title}
		else
			li Empty list!